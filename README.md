### Svelte template project with Tailwind + Postcss and Typescript
To create the template within the selected directory, run the following commands:
```bat
npx run degit jameslinimk/svelte-template
cd svelte-template
npm i
```